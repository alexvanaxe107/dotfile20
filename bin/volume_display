#!/bin/sh

volume=$(pulseaudio-ctl full-status | awk '{print $1}')
if [ "$volume" -eq "0" ]
then
	echo -e " $volume"
elif [ "$volume" -gt "50" ]
then
	echo -e " $volume"
elif [ "$volume" -eq "50" ]
then
	echo -e " $volume"
elif [ "$volume" -lt "50" ]
then
	echo -e " $volume"
fi
