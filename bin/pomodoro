#!/bin/sh

POMODORO=$(pomodoro-client.py status)

if [ -z "$POMODORO" ]; then
	echo "" || exit 1
elif [[ "$POMODORO" = *"PAUSED"* ]]
then
	echo " $POMODORO" || exit 1
else
	echo " $POMODORO" || exit 1
fi

