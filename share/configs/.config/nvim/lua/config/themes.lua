-- lets source the file from the .vim because we must update the other plugins
-- Later we go to this plugins and update that

vim.cmd('source ~/.vim/configs/theme_config.vim')
vim.cmd('source ~/.vim/configs/theme.vim')
